apiVersion: v1
kind: Service
metadata:
  name: nginx
  namespace: todolist-nginx
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: nginx
  ports:
    - name: http
      port: 80
      targetPort: http
